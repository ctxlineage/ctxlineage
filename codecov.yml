coverage:
  status:
    project:
      default:
        informational: true   # whole-repo coverage is advisory, never blocks
    patch:
      default:
        target: 80%           # new/changed lines are held to a real bar
comment:
  layout: "condensed_header, diff"
  require_changes: true
